# LM-QASAS No.1: 作業上の指示

最初にREADME.md、docs/GUIDE_JA.md、docs/SPECIFICATION.md、docs/DECISIONS.md、docs/DEVELOPMENT_LOG.md、docs/WORKFLOW.mdを読む。

- ユーザーと進捗・判断を段階ごとに共有する。2026-09-19、資料差分や未確認事項を要確認と記録し、理由を明示して妥当な実装を進める裁量が与えられた。回答済みの事項を再質問しない。
- ユーザー提供の実データと資料は削除・改変・上書きしない。読み取りとローカル解析は許可されている。元フォルダーの移動も行わない。
- Google Driveにはアクセスしない。ローカルデータの利用許可をクラウドへのアクセス許可と解釈しない。
- このGitHubは公開。実配列、被験者情報、実データ由来の出力・embedding・図、個人別集計・hash、提供資料、認証情報をcommit・pushしない。
- 詳細な実データ監査はGit管理外のlocal_records/、解析結果はoutputs/に置く。標準出力へ実配列を出さない。
- 実データをGitHub Actions等の外部実行環境へ渡さない。公開テスト用データは模擬データであることを明記する。
- 入力cloneはV/J/CDR-H3/isotypeで定義する科学的前提を守る。同一CDR-H3のembeddingキャッシュとclone観測の統合を混同しない。
- Countsを密度重みに使用しない。Countsの単位を推測でread/UMIと断定しない。
- UMAPは可視化専用。候補スコアをUMAP座標から計算しない。
- Top Nは重複しないCDR-H3の種類数。300/500/1000と任意の正整数に対応する。解析時のclone保持と出力時の一意化を分ける。
- 最新の論文・ポスターを主資料、旧修士論文を補助資料とする。差分は要確認として記録する。
- 論文にない値を論文の設定と断定しない。isotype粒度等の暫定判断には理由と状態を明示する。
- コード、手順、検証結果、判断理由、未決定事項を継続的に記録する。機能の未実装と検証の未実施を明確にする。
- Gitへ登録するファイルを明示して内容を確認する。ソースデータを含み得る場所で無確認の一括add/強制addをしない。
- 原本の保護と再現性を優先する。現在のユーザーの明示指示はこの文書より優先する。
- 2026-09-19、ユーザーから日本語の解説書をコードと一緒に継続管理する指示を受けた。引数・設定・出力・検証状態の変更時はGUIDE_JAを同期する。

- 2026-09-20、ユーザーは対応Excelの本番運用でD注釈・D機能を採否に使わない変更を明示承認した。新規解析policyは `takara-rg-hIGH20181210-v2-ignore-d`。V/J F・有効注釈、in-frame、canonical CDR3長5以上、C/isotype条件は維持する。D生情報と元VDJ集計照合は保持するが、Dを採否・clone keyには使わない。
- 旧runは旧policyの記録を保持し、Top Nの再選択や図の再表示だけで新policyへ移行させない。新条件の候補は新規解析で作る。旧D条件を比較する補助解析は、旧policyを明示して再現する。

- 2026-09-20、利用者はD不使用の現行運用を起点に論文図との差を一つずつ確認する方針を指定した。論文をお手本とするが、見た目だけを理由に条件を調整しない。赤点の議論は後の段階とする。第1段階の入力集合は `docs/INPUT_SET_REVIEW.md` に記録し、次のembedding確認と区別する。
