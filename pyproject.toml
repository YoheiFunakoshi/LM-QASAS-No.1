[build-system]
requires = ["setuptools>=78.1.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lm-qasas-local"
version = "0.1.0"
description = "Local LM-QASAS research utilities; candidate selection component"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
